{
  "schema_version": 17,
  "role": "hot_state_authority",
  "project": "astrbot_plugin_volcengine_provider",
  "verdict": {
    "stable_release": "0.1.24",
    "stable_status": "repository_main_and_runtime_published_0_1_24_object_scoped_model_card_video_ui",
    "active_release_candidate": {
      "version": "0.1.25",
      "branch": "fix/0.1.25-transport-guards",
      "pull_request": null,
      "status": "validating",
      "releaseable": false,
      "purpose": "Close the media transport robustness gaps: bound the video transcode wall-clock time, enforce Ark's documented video input ceiling on every local materialization path before base64-encoding, and always terminate a running ffmpeg when a chat request times out or is cancelled (video compressed path and audio WAV normalization)."
    },
    "safe_development_branch": "fix/0.1.25-transport-guards",
    "safe_install_branch": "runtime",
    "marketplace": {
      "listed_version": "0.1.22",
      "status": "last_observed_listing_0_1_22_runtime_0_1_24_published_candidate_0_1_25_not_yet_published"
    },
    "windows_marketplace_install": "0.1.25_unverified"
  },
  "active_experiment": null,
  "development": {
    "version": "0.1.25",
    "branch": "fix/0.1.25-transport-guards",
    "track": "release_candidate",
    "status": "transport_guards_candidate_under_validation",
    "installable": false
  },
  "published_baseline": {
    "version": "0.1.24",
    "main_commit": "8dfb158db1c31872e78e436d0e993d94392462ca",
    "runtime_branch": "runtime",
    "runtime_commit": "c32214ee2af106b324b06595b978f3910ffddac0",
    "runtime_tree": null,
    "dashboard_evidence_run": null,
    "runtime_gate_run": 31913416300,
    "publisher_run": 31913489827,
    "status": "repository_runtime_and_contract_verified"
  },
  "current_goal": {
    "id": "publish-0.1.25-media-transport-guards",
    "goal": "Harden the media transport path without changing any UI, model-card, provider routing or request-override semantics: video transcoding gets a wall-clock timeout and the subprocess is killed on timeout/cancellation; local video materialization enforces Ark's documented video input ceiling before base64-encoding (original and compressed paths, plus cheap data-URL ceiling checks); the audio WAV normalization path also kills its ffmpeg on cancellation.",
    "active_decision_ids": [],
    "non_goals": [
      "Do not change the 0.1.24 object-scoped Video UI contract or any Dashboard bridge behavior.",
      "Do not change Ark payload shapes, provider routing, key pools, retries, or the 0.1.19 request-override semantics.",
      "Do not change the audio 25 MB protocol ceiling or the 120 s audio transcode timeout.",
      "Do not download or probe remote http(s) video URLs locally; their size remains unknown and the 0.1.18 pass-through shape is preserved.",
      "Do not add an ffprobe duration gate or a second compression pipeline.",
      "Do not claim 0.1.25 runtime publication or a real user installation until the publisher and external reinstall are directly observed."
    ]
  },
  "active_validation_frontier": {
    "release": "0.1.25",
    "name": "media_transport_guards",
    "current_stage": "candidate_validating_pending_branch_and_pr_gates",
    "next_actions": [
      "Open the pull request from fix/0.1.25-transport-guards to main and require the Runtime Distribution Gate (including the knowledge-state guard and the new transport-guard test script) to pass.",
      "Mark the candidate ready in the HOT state once all branch/PR gates are green, then merge and let the guarded runtime publisher promote the exact accepted 0.1.25 artifact.",
      "After runtime publication, verify by repository-name reinstall on the user's real AstrBot instance."
    ],
    "observed": [
      "All fourteen packaged unit/contract test scripts pass locally on the candidate tree against a real AstrBot runtime, including the new tests/test_video_transport_guards.py covering the data-URL ceiling, oversized/empty local input rejection before base64, the transcode timeout kill, and cancellation kill contracts for both the video compressed path and the audio normalization path.",
      "The 0.1.24 stable baseline is preserved: main is 8dfb158db1c31872e78e436d0e993d94392462ca and the published runtime branch is c32214ee2af106b324b06595b978f3910ffddac0, both carrying metadata 0.1.24 with the object-scoped model-card Video UI.",
      "The candidate does not modify providers.py, registry.py, capabilities/*, main.py, or the audio 25 MB / 120 s constants; only adapters/video.py, adapters/audio.py and the gate test list changed."
    ]
  },
  "closed_experiments": [
    {
      "id": "0.1.20-source-scoped-video-modality",
      "version": "0.1.20",
      "branch": "fix/source-scoped-video-0.1.20",
      "pull_request": 10,
      "status": "superseded",
      "releaseable": false,
      "merged": false,
      "passed": [
        "minimal runtime artifact boundary",
        "AstrBot 4.26.1 and 4.27.2 plugin loading",
        "Video visible on Ark and Agent Plan create dialogs",
        "Video absent from the foreign OpenAI create dialog"
      ],
      "failed": [
        "The historical final test incorrectly required the retired _volcengine_video_input_mode_ui row after the native Video modality assertion had already passed."
      ],
      "final_failed_run": 31719539311,
      "archive": "docs/archive/EXPERIMENT-0.1.20-source-scoped-video.md",
      "resume_condition": "Satisfied by the 0.1.22 recovery, the 0.1.23 robust delivery layer, the 0.1.24 object-scoped UI, and the 0.1.25 transport guards."
    }
  ],
  "stable_boundaries": [
    "AstrBot owns provider lifecycle, routing, fallback, retry, model-card management, and the base shared Dashboard schema.",
    "The plugin adapts Volcengine request and media protocols; it does not maintain a second global model-capability database.",
    "The user-facing Video truth remains the native per-model-card modalities selection. The precise UI path scopes it to the two plugin-owned Source types after a private dialog clone exists.",
    "No shared-schema Video injection exists anymore. The native video option may only be added on concrete model-card objects of the two plugin-owned Source types, via the private dialog clone (asset bridge) or the live component instance (runtime bridge). Foreign dialogs must never receive it, and the create/update save boundary strips any forged value.",
    "Future AstrBot-native Video support is host-owned and must not be deleted by plugin cleanup.",
    "Request transport settings are not permanent model-capability facts.",
    "Local video materialization must reject empty and oversized input before base64-encoding and bound any ffmpeg work with a timeout plus kill-on-cancel; remote http(s) URLs remain pass-through because their size is unknown locally.",
    "The 0.1.19+ rich per-model request fields, audio path, video quality modes, Agent Plan routing, migrations, and request override semantics are compatibility baseline behavior unless a separate validated change explicitly replaces them.",
    "Development documentation, tests, evidence, and experiments stay outside the allow-list runtime package.",
    "A passing gate proves only the object and layer named by that gate; real source-type differential, persistence, runtime behavior, and unload/release evidence remain distinct checks."
  ],
  "knowledge_lifecycle": {
    "policy": "docs/KNOWLEDGE_LIFECYCLE.md",
    "hot_authority": "docs/PROJECT_STATE.json",
    "warm_navigation": "docs/DECISION_INDEX.json",
    "cold_archive": "docs/archive/",
    "test_ledger": "docs/TEST_HISTORY.md"
  },
  "warning": "Read verdict first, then docs/contracts/MODEL_CARD_VIDEO_CONTRACT.md for any Video/modalities/Provider-UI task. Since 0.1.24 the shared-schema Video fallback is retired: never re-add a global modalities injection. Since 0.1.25 every local video materialization enforces the Ark video input ceiling and every ffmpeg transcode is bounded and killed on timeout/cancellation; do not remove those guards to make a fixture pass."
}
