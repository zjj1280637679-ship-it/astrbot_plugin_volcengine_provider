{
  "schema_version": 19,
  "role": "hot_state_authority",
  "project": "astrbot_plugin_volcengine_provider",
  "verdict": {
    "stable_release": "0.1.25",
    "stable_status": "repository_main_and_runtime_published_0_1_25_media_transport_guards",
    "active_release_candidate": {
      "version": "0.1.26",
      "branch": "fix/0.1.26-update-changelog",
      "pull_request": 19,
      "status": "ready",
      "releaseable": true,
      "purpose": "Ship CHANGELOG.md inside the runtime package so AstrBot's post-update changelog popup renders. AstrBot's plugin service reads CHANGELOG.md from the installed plugin directory; the runtime allow-list previously excluded it, leaving the update dialog empty. The shipped file stays identical to the development changelog."
    },
    "safe_development_branch": "fix/0.1.26-update-changelog",
    "safe_install_branch": "runtime",
    "marketplace": {
      "listed_version": "0.1.22",
      "status": "last_observed_listing_0_1_22_runtime_0_1_25_published_candidate_0_1_26_not_yet_published"
    },
    "windows_marketplace_install": "0.1.26_unverified"
  },
  "active_experiment": null,
  "development": {
    "version": "0.1.26",
    "branch": "fix/0.1.26-update-changelog",
    "track": "release_candidate",
    "status": "update_changelog_candidate_all_pr_gates_green",
    "installable": false
  },
  "published_baseline": {
    "version": "0.1.25",
    "main_commit": "8e87a7eae1356c2b257264d878c40d0805143464",
    "runtime_branch": "runtime",
    "runtime_commit": "2cce38ffc389930854e5d7752cac67a441741318",
    "runtime_tree": null,
    "dashboard_evidence_run": 31914581988,
    "runtime_gate_run": 31914581993,
    "publisher_run": 31914641554,
    "status": "repository_runtime_and_contract_verified"
  },
  "current_goal": {
    "id": "publish-0.1.26-update-changelog",
    "goal": "Make the AstrBot post-update changelog popup render by shipping CHANGELOG.md in the runtime package. The runtime allow-list, the default-branch export hygiene, the publisher allow-list, and the release specification must all admit the same file, and the runtime tree change must ride a strictly newer version (0.1.26) through the normal gate and publisher flow.",
    "active_decision_ids": [],
    "non_goals": [
      "Do not ship README.md, docs/ or any other development-only material with it.",
      "Do not split the shipped changelog from the development changelog; they must remain one file.",
      "Do not change any plugin runtime behavior, UI bridge, or request semantics.",
      "Do not claim 0.1.26 runtime publication or a real user installation until the publisher and external reinstall are directly observed."
    ]
  },
  "active_validation_frontier": {
    "release": "0.1.26",
    "name": "update_changelog_distribution",
    "current_stage": "candidate_ready_all_pr_gates_green_pending_main_merge_and_runtime_publish",
    "next_actions": [
      "Merge PR #19 into main, then require the main-branch Runtime Distribution Gate to pass.",
      "Allow the guarded runtime publisher to promote the exact accepted 0.1.26 runtime artifact (this promotion changes the runtime tree by adding CHANGELOG.md).",
      "After runtime publication, verify by repository-name reinstall that the post-update changelog popup renders."
    ],
    "observed": [
      "AstrBot's PluginService.get_plugin_changelog reads CHANGELOG.md (or changelog.md/CHANGELOG/changelog) from the installed plugin directory; the 0.1.25 runtime allow-list excludes it, so the update dialog has no content.",
      "PR #19 gates are green: Runtime Distribution Gate run 31916099001 (artifact boundary including default-archive equivalence with 27 files, AstrBot 4.26.1 / 4.27.2 packaged loads and the fourteen contract scripts).",
      "Locally, the updated allow-list builder packages CHANGELOG.md (zip 628 KB within budget) and the default-branch archive equivalence check passes.",
      "The knowledge-state guard passes with development=0.1.26 and the ready candidate."
    ]
  },
  "closed_experiments": [
    {
      "id": "0.1.20-source-scoped-video-modality",
      "version": "0.1.20",
      "branch": "fix/source-scoped-video-0.1.20",
      "pull_request": 10,
      "status": "superseded",
      "releaseable": false,
      "merged": false,
      "passed": [
        "minimal runtime artifact boundary",
        "AstrBot 4.26.1 and 4.27.2 plugin loading",
        "Video visible on Ark and Agent Plan create dialogs",
        "Video absent from the foreign OpenAI create dialog"
      ],
      "failed": [
        "The historical final test incorrectly required the retired _volcengine_video_input_mode_ui row after the native Video modality assertion had already passed."
      ],
      "final_failed_run": 31719539311,
      "archive": "docs/archive/EXPERIMENT-0.1.20-source-scoped-video.md",
      "resume_condition": "Satisfied by the 0.1.22 recovery, the 0.1.23 robust delivery layer, the 0.1.24 object-scoped UI, the 0.1.25 transport guards, and the 0.1.26 update-changelog distribution."
    }
  ],
  "stable_boundaries": [
    "AstrBot owns provider lifecycle, routing, fallback, retry, model-card management, and the base shared Dashboard schema.",
    "The plugin adapts Volcengine request and media protocols; it does not maintain a second global model-capability database.",
    "The user-facing Video truth remains the native per-model-card modalities selection. The precise UI path scopes it to the two plugin-owned Source types after a private dialog clone exists.",
    "No shared-schema Video injection exists anymore. The native video option may only be added on concrete model-card objects of the two plugin-owned Source types, via the private dialog clone (asset bridge) or the live component instance (runtime bridge). Foreign dialogs must never receive it, and the create/update save boundary strips any forged value.",
    "Future AstrBot-native Video support is host-owned and must not be deleted by plugin cleanup.",
    "Request transport settings are not permanent model-capability facts.",
    "Local video materialization must reject empty and oversized input before base64-encoding and bound any ffmpeg work with a timeout plus kill-on-cancel; remote http(s) URLs remain pass-through because their size is unknown locally.",
    "CHANGELOG.md ships with the runtime package because AstrBot renders the post-update changelog popup from it; it stays byte-identical to the development changelog and must not carry secrets.",
    "The 0.1.19+ rich per-model request fields, audio path, video quality modes, Agent Plan routing, migrations, and request override semantics are compatibility baseline behavior unless a separate validated change explicitly replaces them.",
    "Development documentation, tests, evidence, and experiments stay outside the allow-list runtime package.",
    "A passing gate proves only the object and layer named by that gate; real source-type differential, persistence, runtime behavior, and unload/release evidence remain distinct checks."
  ],
  "knowledge_lifecycle": {
    "policy": "docs/KNOWLEDGE_LIFECYCLE.md",
    "hot_authority": "docs/PROJECT_STATE.json",
    "warm_navigation": "docs/DECISION_INDEX.json",
    "cold_archive": "docs/archive/",
    "test_ledger": "docs/TEST_HISTORY.md"
  },
  "warning": "Read verdict first, then docs/contracts/MODEL_CARD_VIDEO_CONTRACT.md for any Video/modalities/Provider-UI task. 0.1.26 is a distribution-only candidate: it changes the runtime package contents (adds CHANGELOG.md for AstrBot's update-log popup) without touching plugin behavior. A changed runtime tree requires the strictly newer version plus a new ready candidate before the publisher promotes it."
}
